__pycache__/
*.pyc
.env
results.json
metrics.json
.DS_Store

# Dataset: keep the folder structure (.gitkeep) but don't commit the images
data/**/*.jpg
data/**/*.jpeg
data/**/*.png
data/**/*.webp

# Task brief / supplied documents - not ours to redistribute
*.pdf
*.pptx
*.xlsx
*.xls

# Local SQLite: the agent's tag cache and the results database
*.sqlite3

# Per-run records; keep the folder, not the output
results/*.json
