# Content Analysis Agent - runtime dependencies
# Install with:  pip install -r requirements.txt
# Tested on Python 3.11.

# The agent itself: the prepare -> analyze_image -> persist state machine.
langgraph>=1.2,<2

# Claude vision. Imported lazily in vlm.py, and again in tools/search.py for
# --search-tool anthropic, so nothing loads it until a run calls out.
anthropic>=1.2,<2

# Downscales images before upload (vlm.encode_image); also a Streamlit dep.
pillow>=12,<13

# Streamlit demo UI (app/streamlit_app.py).
streamlit>=1.62,<2

# Only for the `insights` command, which reads the metadata sheet. Imported
# lazily in metadata.py, so tagging and evaluation run without them.
pandas>=2.0
openpyxl>=3.1,<4

# Test suite (offline; no API key needed).
pytest>=8.0

# Reads ANTHROPIC_API_KEY from a local .env file.
python-dotenv>=1.2,<2
